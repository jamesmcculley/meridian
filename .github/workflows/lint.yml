name: Python Quality
on: [push, pull_request]

jobs:
  python-quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Check for Python files
        id: check
        run: |
          count=$(find tools/ -name "*.py" | wc -l)
          echo "count=$count" >> $GITHUB_OUTPUT

      - name: Install meridian-detect dev dependencies
        if: steps.check.outputs.count > 0
        working-directory: tools/meridian-detect
        run: python -m pip install -e ".[dev]"

      - name: Lint Python
        if: steps.check.outputs.count > 0
        run: python -m ruff check tools/

      - name: Type check Python
        if: steps.check.outputs.count > 0
        run: python -m mypy tools/meridian-detect/src

      - name: Test Python
        if: steps.check.outputs.count > 0
        run: python -m pytest tools/meridian-detect/tests

      - name: Skip Python checks
        if: steps.check.outputs.count == 0
        run: echo "No Python files in tools/ — skipping Python checks"
